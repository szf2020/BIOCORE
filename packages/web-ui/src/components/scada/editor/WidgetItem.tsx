'use client';
import React from 'react';
import type { WidgetDef } from '@/widgets';
export function WidgetItem(_props: { widget: WidgetDef; isSelected: boolean; onSelect: (id: string) => void; onMove: (id: string, x: number, y: number) => void; onResize: (id: string, w: number, h: number) => void }) { return null; }
